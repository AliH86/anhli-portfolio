# Garden local route stability · 14 September 2026

User accepted combined direction (“ok rồi, thấy cũng ổn, tiếp tục, kỳ vọng nó sẽ hài hoà ổn định”). Integrated the accepted static Garden into actual local root and route generation. Review http://127.0.0.1:8784/ . This supersedes the earlier QA-only/default-route gap. No public deployment or final 3D art acceptance.

Changes: new `js/redesign/garden-plate.mjs`, `css/garden-plate.css`; view.mjs renders a responsive Garden picture on the root; shell.mjs mounts a route-aware controller. Builder regenerated all eight routes. Source art, geometry, music data and legacy engine remain unchanged (preservation receipt proves engine equivalence after removing generated shell/base/title/new CSS link). All route bodies were compared before build and had no independent edits.

Cold Music/other routes do not load Garden images. Returning to Garden lazily creates one picture, retains one actual audio/player and holds image error state across routes. Root reload/new tab now retain the Garden. Failed image shows a light fallback with readable real UI. Portrait tablet selects portrait art; short screens scroll an 850px composition instead of squeezing text over trees. Redundant scroll cue hidden below1100px. Old separate QA document controller is preserved without duplicate scene mount.

23 checks pass: 13 route regression checks in `scripts/test-garden-routes.mjs`, including real playback through routes/MAP, seek/mute/pause, 26 albums, all eight direct routes, Back/Forward/reload, focus/keyboard, lightbox/Sky, responsive and no-JS/reduced motion; 10 new checks in `scripts/test-garden-stability.mjs`, including cold mobile image selection, repeat navigation, image failure, five viewport sizes and no-JS path via Flat to Music. One old MAP test selector matched both MUSIC and /SAP; narrowed the test to the destination link, no duplicate-navigation product change.

Evidence in `docs/qa/garden-stable-2026-09-14/` and regression subfolder. Final small CSS change positions the review caption at document bottom on short screens; core tests ran just before that one presentation-only line. Prior reviewed screenshots show the short-screen caption at viewport bottom; all layout and behavior fixes were tested. Re-capture short-screen screenshot next time if needed.

No commit/push/deploy. CPU/browser emulation is not actual-device GPU or Safari acceptance. Final material/lighting/host matte edges remain polish work; do not infer other-state motion approval. Usage reached96% five-hour/68% weekly at wrap-up; no reset credit used. Stop new heavy work in this window. Next: visual review of stable root on actual devices; target any remaining layout/art issue, then final Garden approval and deliberate release checks.
