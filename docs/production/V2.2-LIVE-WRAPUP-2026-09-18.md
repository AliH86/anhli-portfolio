# Vườn của Li · V2.2 đã live · 18/09/2026

Anh đã duyệt art và yêu cầu: “Tuyệt, lên live đi em. Rồi a check trên mobile/iphone cho”. Chỉ đạo này thay thế trạng thái chờ duyệt/no-deploy trong tài liệu lịch sử.

- Live: https://alih86.github.io/anhli-portfolio/?v=22-art2
- Commit: `d00e69a94d2c91d763ce4eff8e025417524ef343`, đã ở `origin/main`.
- GitHub Pages build/deploy thành công: https://github.com/AliH86/anhli-portfolio/actions/runs/35303812328
- Bản phát hành khớp đủ547 file trong checkpoint art đã duyệt. Root entry chỉ thêm `<base href="./garden-v2/">` cho đường dẫn Pages.
- Nội dung gồm art sáng hơn, khung gỗ/dây leo/hoa lá, giấy trong với chữ đậm, bố cục kệ/player/playlist, thư và bảng ảnh. Catalog canonical26 album/213 bài;171 ảnh/18 video. Không thêm karaoke/lời giả.

## Xác minh sau phát hành

-43/43 regression tests đạt; kiểm tra package dưới prefix `/anhli-portfolio/` trước khi push.
-56 tài nguyên live (HTML, code, CSS, JSON, font, ảnh) trả thành công và khớp SHA-256 bản release.
- Site thật: nhấn chuông vào vườn, mở sạp, không tự phát; bấm Play phát nhạc R2 và tiến độ tăng. Một native audio; đóng sạp nhạc vẫn chạy. Đã dừng nhạc sau lượt thử.
- Playlist mở đúng trong viewport390×844; ảnh `live-mobile-playlist.png` lưu cạnh release receipt. Không có lỗi/warning console hoặc ảnh đã tải bị hỏng trong lượt smoke. Đã reset viewport khi xong.
- Đây là kiểm tra browser desktop và khung mobile. iPhone/Safari thật, khóa màn hình, chuyển ứng dụng, Bluetooth, mạng chậm, nhiệt/pin vẫn chờ anh thử; không suy ra từ viewport.

## Mốc tiếp quản

- Runtime nguồn tiếp tục: `fresh/dist/`; không chỉnh runtime thêm trong lượt phát hành.
- Checkpoint: `fresh/checkpoints/2026-09-18-v2.2-art-review/`; ZIP/hashes gốc giữ nguyên. `acceptance.json` và `publication.json` mới xác nhận duyệt/live; `receipt.json` cũ là snapshot trước phát hành.
- Release worktree sạch: `../portfolio-garden-v22-release-2026-09-18/`, branch `codex/garden-v2-2-release-2026-09-18`.
- Gói và evidence: `../website-releases/2026-09-18-v2.2/`, gồm `site.zip`, hashes, QA, acceptance, release/build/live/browser/publication receipts.
- Bản trước V2.1 hotfix: `33f1c0c1f4e968781a4c2674797363d1b332059e`, worktree/archive giữ nguyên. Nếu cần rollback, tạo commit revert từ main mới nhất; không reset/force-push.
- Development checkout vẫn có công việc cũ chưa commit, được giữ nguyên. Không pull/reset hoặc stage hàng loạt. Không chạy legacy extractor ghi đè canonical catalog.

Bước tiếp theo: nhận phản hồi iPhone của anh rồi xử lý lỗi cụ thể. Không tự mở thêm vòng chỉnh art hoặc phát hành khác khi chưa có yêu cầu.
